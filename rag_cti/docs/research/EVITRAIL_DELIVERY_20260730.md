# EviTRAIL delivery status — 2026-07-30

Consumer revision:
`Mitraaaaa/Evitrial@da4a29e8ce25cff8cbddebb444b069296f949511`

This delivery is code-backed. The JSON/JSONL artifacts below are generated by
the checked-in scripts; they are not the only deliverable.

Published branch:
`codex/evitrail-delivery-20260730`

The ready data artifacts are versioned with Git LFS under
`rag_cti/data/evitrail_delivery_20260730/` on that branch.

## Ready: non-OTX factual base

Data:
`F:\DATA_COLLECTION\evitrail_source_alignment_20260730\handoff_v5`

Validation:
`F:\DATA_COLLECTION\evitrail_source_alignment_20260730\validation_v5_agent.json`

The strict handoff contains exactly the five files accepted by
`read_handoff()`:

- `events.jsonl`: 32,056
- `nodes.jsonl`: 493,734
- `edges.jsonl`: 603,602
- `source_claims.jsonl`: 126,181
- `rejected_records.jsonl`: 25,794

The exact current reader materialized 520,489 indicator observations and
83,113 relations, exactly matching all 603,602 edge rows. This includes all 18
CIRCL MISP Object IP-to-ASN relations found in the collected raw objects. A
four-source, seven-stage current-pipeline smoke passed, and 787,633 reference
fields contained no absolute filesystem path.

Rebuild:

```powershell
python scripts/build_evitrail_source_handoff.py `
  --raw-root <cti-rag>/data/raw `
  --processed-root <cti-rag>/data/processed/trail_multisource_part1_v1_20260724 `
  --output-dir F:\DATA_COLLECTION\<version>\handoff `
  --work-dir F:\DATA_COLLECTION\<version>\work
```

Validate:

```powershell
python scripts/validate_evitrail_source_handoff.py `
  --handoff F:\DATA_COLLECTION\<version>\handoff `
  --evitrail-root <Evitrial checkout at da4a29e> `
  --out F:\DATA_COLLECTION\<version>\validation.json
```

## Ready: normalized existing OTX enrichment inventory

Data and manifests:
`F:\DATA_COLLECTION\evitrail_delivery_20260730\outputs\otx_enrichment_4505_v1`

The complete 25,985-task terminal-state ledger was normalized to 529,782
EviTRAIL cached-infrastructure observations. The exact current reader loaded
all rows and produced:

- 346,562 domain-to-IP relations;
- 336,356 IP-to-ASN relations;
- 23,012 written, 39 reused, 2,872 empty, and 62 retry-exhausted task
  outcomes, with empty and retry-exhausted evidence retained explicitly.

This inventory was derived from the deduplicated infrastructure seeds of the
previously selected 4,505-Pulse `paper_model_22` population. It is not
enrichment coverage of the latest 17,454-Pulse OTX snapshot. The manifest and
validation therefore declare `scope=partial` and
`full_snapshot_coverage=false`.

Normalize:

```powershell
python scripts/normalize_evitrail_otx_enrichment.py `
  --ledger <terminal-state-ledger.jsonl> `
  --output F:\DATA_COLLECTION\<version>\otx_enrichment.jsonl `
  --manifest F:\DATA_COLLECTION\<version>\manifest.json `
  --evitrail-root <Evitrial checkout at da4a29e> `
  --subset-pulse-count 4505 `
  --snapshot-pulse-count 17454
```

## In progress: latest OTX factual base and global vocabulary

The latest immutable raw OTX snapshot contains 17,454 Pulses. Its
occurrence-aware sharded factual-base build and exact-reader validation are in
progress. Actor-like tags and collection queries remain provenance-only and
cannot vote on attribution. The final global vocabulary will be generated
only from resolved factual attribution claims, with MITRE ATT&CK and Malpedia
used for identity/alias resolution rather than as Event attribution sources.
